// This layout is intentionally empty — admin split is handled by (app)/layout.tsx AppShell
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
